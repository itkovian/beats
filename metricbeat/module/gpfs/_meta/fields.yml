- key: gpfs
  title: "Gpfs"
  description: >
    gpfs usage metrics, collected from the storage backend
  fields:
    - name: gpfs
      type: group
      description: >
        `gpfs` contains GPFS metrics
      fields:
